<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Pixelletter — Make Your Magical Letters</title>
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323:wght@400&family=Silkscreen:wght@400;700&family=Pixelify+Sans:wght@400;700&family=Orbitron:wght@400;700&family=Space+Mono:wght@400;700&family=Courier+Prime:wght@400;700&family=Josefin+Sans:wght@400;700&family=Playfair+Display:wght@400;700&family=Abril+Fatface&family=Permanent+Marker&family=Caveat:wght@400;700&family=Dancing+Script:wght@400;700&family=Creepster&family=Righteous&display=swap" rel="stylesheet">
<style>
:root {
  --px: 4px;
  --green: #5aad5a;
  --green-dark: #3d7a3d;
  --brown: #8b5e3c;
  --brown-dark: #5c3d26;
  --sky: #6ec1e4;
  --gold: #f9c22b;
  --gold-dark: #c49a1b;
  --diamond: #5de8f0;
  --redstone: #e83232;
  --cream: #fffde7;
  --paper: #faf3dc;
  --ink: #2d1e0f;
  --panel: #d4c5a0;
  --panel-dark: #b8a882;
  --ui-dark: #1a0f05;
  --ui-mid: #3d2e1e;
  --ui-light: #6e5a3e;
  --shadow: rgba(0,0,0,0.5);
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: 'Press Start 2P', monospace;
  background: #1a0f05;
  min-height: 100vh;
  overflow-x: hidden;
  image-rendering: pixelated;
}

body::before {
  content: '';
  position: fixed;
  inset: 0;
  background: 
    radial-gradient(1px 1px at 10% 15%, #fff 0%, transparent 100%),
    radial-gradient(1px 1px at 25% 40%, #ffd 0%, transparent 100%),
    radial-gradient(2px 2px at 50% 10%, #fff 0%, transparent 100%),
    radial-gradient(1px 1px at 70% 60%, #fff 0%, transparent 100%),
    radial-gradient(1px 1px at 85% 25%, #fff 0%, transparent 100%),
    radial-gradient(2px 2px at 90% 75%, #ffd 0%, transparent 100%),
    radial-gradient(1px 1px at 35% 80%, #fff 0%, transparent 100%),
    radial-gradient(1px 1px at 60% 45%, #fff 0%, transparent 100%),
    radial-gradient(2px 2px at 15% 65%, #ffd 0%, transparent 100%),
    linear-gradient(180deg, #0a0520 0%, #1a0a30 30%, #0d0510 60%, #1a0f05 100%);
  pointer-events: none;
  z-index: 0;
}

.px-btn {
  font-family: 'Press Start 2P', monospace;
  font-size: 9px;
  color: #fff;
  background: var(--green-dark);
  border: none;
  padding: 8px 12px;
  cursor: pointer;
  position: relative;
  image-rendering: pixelated;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  outline: none;
  box-shadow: inset -3px -3px 0 rgba(0,0,0,0.4), inset 3px 3px 0 rgba(255,255,255,0.15);
  transition: filter 0.1s;
}
.px-btn:hover { filter: brightness(1.2); }
.px-btn:active { box-shadow: inset 3px 3px 0 rgba(0,0,0,0.4), inset -3px -3px 0 rgba(255,255,255,0.1); transform: translate(1px,1px); }
.px-btn.gold { background: var(--gold-dark); color: #2d1e0f; }
.px-btn.red { background: #8b1a1a; }
.px-btn.blue { background: #1a4a8b; }
.px-btn.purple { background: #5a1a8b; }
.px-btn.sm { font-size: 7px; padding: 5px 8px; }
.px-btn.lg { font-size: 11px; padding: 12px 20px; }
.px-btn.active-tool { background: var(--gold-dark); color: #1a0f05; box-shadow: inset 3px 3px 0 rgba(0,0,0,0.4); }

.px-panel { background: var(--panel); border: 4px solid #000; box-shadow: 4px 4px 0 #000, inset 0 0 0 2px var(--panel-dark); }
.px-panel-dark { background: var(--ui-mid); border: 4px solid #000; box-shadow: 4px 4px 0 #000; }

header {
  position: relative;
  z-index: 10;
  text-align: center;
  padding: 24px 16px 16px;
}

.logo {
  display: inline-block;
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(20px, 4vw, 36px);
  color: var(--gold);
  text-shadow: 4px 4px 0 var(--gold-dark), 8px 8px 0 #000;
  letter-spacing: 2px;
  animation: pixelPulse 2s ease-in-out infinite;
}

@keyframes pixelPulse {
  0%,100% { text-shadow: 4px 4px 0 var(--gold-dark), 8px 8px 0 #000; }
  50% { text-shadow: 4px 4px 0 var(--gold-dark), 8px 8px 0 #000, 0 0 20px var(--gold); }
}

.tagline { font-family: 'VT323', monospace; font-size: 20px; color: #a0d8a0; margin-top: 8px; letter-spacing: 1px; }

.app {
  position: relative;
  z-index: 5;
  display: grid;
  grid-template-columns: 300px 1fr;
  gap: 16px;
  padding: 0 16px 24px;
  max-width: 1400px;
  margin: 0 auto;
  align-items: start;
}

@media (max-width: 900px) { .app { grid-template-columns: 1fr; } }

.sidebar { display: flex; flex-direction: column; gap: 12px; }

.tool-section { padding: 12px; }

.section-title {
  font-size: 8px;
  color: var(--gold);
  border-bottom: 2px solid var(--gold-dark);
  padding-bottom: 6px;
  margin-bottom: 10px;
  text-shadow: 1px 1px 0 #000;
}

/* COLOR PICKER */
.color-grid { display: grid; grid-template-columns: repeat(8, 1fr); gap: 3px; margin-bottom: 8px; }
.color-swatch { width: 100%; aspect-ratio: 1; cursor: pointer; border: 2px solid transparent; transition: transform 0.1s; }
.color-swatch:hover { transform: scale(1.2); border-color: #fff; }
.color-swatch.selected { border-color: #fff; box-shadow: 0 0 0 2px #000; }

/* BG COLOR PICKER */
.bg-color-grid { display: grid; grid-template-columns: repeat(8, 1fr); gap: 3px; margin-bottom: 8px; }
.bg-color-swatch { width: 100%; aspect-ratio: 1; cursor: pointer; border: 2px solid transparent; transition: transform 0.1s; }
.bg-color-swatch:hover { transform: scale(1.2); border-color: #fff; }
.bg-color-swatch.selected { border-color: #fff; box-shadow: 0 0 0 2px #000; }

/* FONT SELECTOR */
.font-grid { display: flex; flex-direction: column; gap: 4px; max-height: 200px; overflow-y: auto; }
.font-opt { padding: 6px 8px; cursor: pointer; font-size: 14px; background: var(--ui-mid); color: #e0d4b8; border: 2px solid transparent; transition: background 0.1s; }
.font-opt:hover { background: var(--ui-light); }
.font-opt.selected { border-color: var(--gold); background: var(--ui-light); }

/* STICKER GRID */
.sticker-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 4px; }
.sticker-btn { font-size: 20px; padding: 4px; cursor: pointer; background: var(--ui-mid); border: 2px solid transparent; text-align: center; transition: background 0.1s, transform 0.1s; line-height: 1.4; }
.sticker-btn:hover { background: var(--ui-light); transform: scale(1.15); }

.px-slider { width: 100%; accent-color: var(--gold); height: 20px; cursor: pointer; }
.size-display { font-size: 9px; color: var(--gold); text-align: right; margin-top: 4px; }

/* THEME TABS */
.theme-tabs { display: flex; flex-wrap: wrap; gap: 4px; margin-bottom: 8px; }
.theme-tab { font-family: 'Press Start 2P', monospace; font-size: 6px; padding: 4px 6px; cursor: pointer; background: var(--ui-mid); color: #e0d4b8; border: 2px solid transparent; transition: all 0.1s; }
.theme-tab:hover { background: var(--ui-light); }
.theme-tab.active { border-color: var(--gold); color: var(--gold); }

/* CANVAS SIZE SELECTOR */
.ratio-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 5px; margin-bottom: 8px; }
.ratio-btn { font-family: 'Press Start 2P', monospace; font-size: 6px; padding: 6px 4px; cursor: pointer; background: var(--ui-mid); color: #e0d4b8; border: 2px solid transparent; text-align: center; transition: all 0.1s; line-height: 1.6; }
.ratio-btn:hover { background: var(--ui-light); }
.ratio-btn.active { border-color: var(--gold); color: var(--gold); background: var(--ui-light); }

/* LETTER CANVAS */
.letter-area { display: flex; flex-direction: column; gap: 12px; position: sticky; top: 12px; }

.canvas-wrapper { position: relative; overflow: hidden; display: flex; align-items: flex-start; justify-content: flex-start; }

.letter-paper {
  position: relative;
  font-family: 'Press Start 2P', monospace;
  word-wrap: break-word;
  overflow: hidden;
  cursor: crosshair;
  outline: none;
  flex-shrink: 0;
  transition: background 0.3s;
}

/* Themes */
.theme-default { background: #faf3dc; }
.theme-night { background: #0d0730; color: #c4b5fd; }
.theme-marvel { background: #8b0000; color: #ffd700; }
.theme-nature { background: #e8f5e9; }
.theme-ocean { background: #0077b6; color: #fff; }
.theme-candy { background: linear-gradient(135deg, #ffb3c6 0%, #ffd6a5 50%, #caffbf 100%); }
.theme-galaxy { background: #0a0020; color: #e0c9ff; }
.theme-retro { background: #1a0000; color: #ff6600; }
.theme-pastel { background: #fff0f5; }
.theme-parchment { background: #f5e6c8; }
.theme-sunset { background: linear-gradient(135deg, #ff6b6b, #ffd93d, #ff8c42); }
.theme-mint { background: #d4f5e9; }
.theme-lavender { background: #e8d5f5; }
.theme-midnight { background: #0a0a2e; color: #64ffda; }
.theme-forest { background: #1a3d1a; color: #a8ff78; }
.theme-rose { background: linear-gradient(135deg, #fce4ec, #f8bbd9); }
.theme-arctic { background: linear-gradient(135deg, #e0f7fa, #b2ebf2, #e0f7fa); }
.theme-lava { background: linear-gradient(135deg, #1a0000, #3d0000); color: #ff4500; }

/* Text on paper */
.letter-paper .text-content {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  z-index: 2;
  line-height: 1.4;
  word-break: break-word;
  white-space: pre-wrap;
  outline: none;
  padding: 24px;
  cursor: text;
  height: 100%;
  overflow-y: auto;
}
.letter-paper .text-content::-webkit-scrollbar { width: 5px; }
.letter-paper .text-content::-webkit-scrollbar-track { background: rgba(0,0,0,0.08); }
.letter-paper .text-content::-webkit-scrollbar-thumb { background: rgba(0,0,0,0.22); border-radius: 3px; }

/* Stickers on canvas */
.letter-sticker {
  position: absolute;
  cursor: grab;
  user-select: none;
  z-index: 10;
  font-size: 32px;
  touch-action: none;
  transition: outline 0.1s;
  transform-origin: center center;
}
/* Uploaded image inside sticker — width controlled by JS only */
.letter-sticker img {
  display: block;
  width: 80px;
  height: auto;
  max-width: none !important;
  max-height: none !important;
  pointer-events: none;
  user-select: none;
}
.letter-sticker:hover { outline: 2px dashed rgba(255,255,255,0.5); }
.letter-sticker.dragging { cursor: grabbing; z-index: 100; opacity: 0.85; }
.letter-sticker.selected { outline: 2px dashed var(--gold) !important; }

.sticker-controls {
  position: absolute;
  top: -28px;
  left: 50%;
  transform: translateX(-50%);
  display: none;
  gap: 4px;
  background: rgba(0,0,0,0.8);
  padding: 3px 5px;
  border: 1px solid var(--gold);
  white-space: nowrap;
  z-index: 200;
}
.letter-sticker.selected .sticker-controls { display: flex; }

.sticker-ctrl-btn {
  background: #333;
  color: #fff;
  border: none;
  font-size: 10px;
  width: 20px;
  height: 20px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: monospace;
}
.sticker-ctrl-btn:hover { background: var(--gold); color: #000; }

.del-btn { position: absolute; top: -8px; right: -8px; width: 18px; height: 18px; background: #e83232; color: #fff; font-size: 11px; border: none; cursor: pointer; display: none; align-items: center; justify-content: center; font-family: monospace; line-height: 1; font-weight: bold; z-index: 20; }
.letter-sticker:hover .del-btn { display: flex; }

/* ── IMAGE STICKER — Canva-style bounding box ── */
.img-sticker {
  position: absolute;
  cursor: grab;
  user-select: none;
  z-index: 10;
  touch-action: none;
  transform-origin: center center;
}
.img-sticker:active { cursor: grabbing; }
.img-sticker img {
  display: block;
  width: 80px;
  height: auto;
  max-width: none !important;
  max-height: none !important;
  pointer-events: none;
  user-select: none;
}

/* Purple selection border — only when selected */
.img-sticker .img-selection-box {
  position: absolute;
  inset: 0;
  border: 2px solid #7c4dff;
  pointer-events: none;
  display: none;
  box-sizing: border-box;
}
.img-sticker.img-selected .img-selection-box { display: block; }

/* 8 resize handles */
.img-handle {
  position: absolute;
  width: 10px;
  height: 10px;
  background: #fff;
  border: 2px solid #7c4dff;
  border-radius: 50%;
  z-index: 40;
  display: none;
  touch-action: none;
  box-sizing: border-box;
}
.img-sticker.img-selected .img-handle { display: block; }
/* corners */
.img-handle.nw { top: -5px;  left: -5px;  cursor: nw-resize; }
.img-handle.ne { top: -5px;  right: -5px; cursor: ne-resize; }
.img-handle.sw { bottom: -5px; left: -5px;  cursor: sw-resize; }
.img-handle.se { bottom: -5px; right: -5px; cursor: se-resize; }
/* edges */
.img-handle.n  { top: -5px;    left: calc(50% - 5px); cursor: n-resize; }
.img-handle.s  { bottom: -5px; left: calc(50% - 5px); cursor: s-resize; }
.img-handle.w  { top: calc(50% - 5px); left: -5px;  cursor: w-resize; }
.img-handle.e  { top: calc(50% - 5px); right: -5px; cursor: e-resize; }

/* Rotation handle below */
.img-rot-line {
  position: absolute;
  bottom: -28px;
  left: 50%;
  transform: translateX(-50%);
  width: 2px;
  height: 20px;
  background: #7c4dff;
  display: none;
  pointer-events: none;
}
.img-sticker.img-selected .img-rot-line { display: block; }

.img-rot-handle {
  position: absolute;
  bottom: -46px;
  left: 50%;
  transform: translateX(-50%);
  width: 20px;
  height: 20px;
  background: #fff;
  border: 2px solid #7c4dff;
  border-radius: 50%;
  cursor: grab;
  display: none;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  z-index: 40;
  touch-action: none;
  color: #7c4dff;
}
.img-sticker.img-selected .img-rot-handle { display: flex; }
.img-rot-handle:active { cursor: grabbing; }

/* delete btn */
.img-sticker .img-del-btn {
  position: absolute;
  top: -10px;
  right: -10px;
  width: 20px;
  height: 20px;
  background: #e83232;
  color: #fff;
  font-size: 13px;
  border: none;
  border-radius: 50%;
  cursor: pointer;
  display: none;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  z-index: 50;
  line-height: 1;
}
.img-sticker.img-selected .img-del-btn { display: flex; }


.pixel-border { border: 4px solid #000; box-shadow: 0 0 0 4px var(--panel-dark), 8px 8px 0 #000; }

/* TOOLBAR */
.editor-toolbar { display: flex; flex-wrap: wrap; gap: 6px; padding: 10px 12px; align-items: center; }
.toolbar-sep { width: 2px; height: 24px; background: var(--ui-light); margin: 0 4px; }

/* MODAL */
.modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 1000; display: none; align-items: center; justify-content: center; padding: 20px; }
.modal-overlay.open { display: flex; }
.modal-box { background: var(--paper); max-width: 700px; width: 100%; max-height: 85vh; overflow-y: auto; padding: 20px; position: relative; border: 4px solid #000; box-shadow: 8px 8px 0 #000; }

.share-box { background: var(--ui-dark); border: 2px solid var(--gold-dark); padding: 8px 12px; font-family: 'VT323', monospace; font-size: 16px; color: var(--gold); width: 100%; word-break: break-all; cursor: pointer; letter-spacing: 0.5px; }

.notif { position: fixed; bottom: 20px; right: 20px; z-index: 9999; font-family: 'Press Start 2P', monospace; font-size: 9px; color: #fff; background: var(--green-dark); padding: 10px 14px; border: 3px solid #000; box-shadow: 4px 4px 0 #000; animation: slideIn 0.3s ease; max-width: 240px; }
.notif.error { background: #8b1a1a; }
@keyframes slideIn { from { transform: translateX(100px); opacity: 0; } to { transform: translateX(0); opacity: 1; } }

.px-label { font-size: 7px; color: #c0b090; margin-bottom: 4px; display: block; }

::-webkit-scrollbar { width: 8px; }
::-webkit-scrollbar-track { background: var(--ui-dark); }
::-webkit-scrollbar-thumb { background: var(--ui-light); }

.letter-paper .text-content::-webkit-scrollbar { width: 6px; }
.letter-paper .text-content::-webkit-scrollbar-track { background: rgba(0,0,0,0.1); }
.letter-paper .text-content::-webkit-scrollbar-thumb { background: rgba(0,0,0,0.25); border-radius: 3px; }

/* TABS */
.sidebar-tabs { display: flex; gap: 0; }
.side-tab { flex: 1; font-family: 'Press Start 2P', monospace; font-size: 6px; padding: 7px 2px; cursor: pointer; background: var(--ui-dark); color: #888; border: 2px solid #000; border-bottom: none; text-align: center; transition: background 0.1s; }
.side-tab.active { background: var(--panel); color: var(--ink); }

.upload-zone { border: 3px dashed var(--ui-light); padding: 12px 8px; text-align: center; cursor: pointer; color: #a09070; font-size: 7px; line-height: 1.8; transition: border-color 0.2s; }
.upload-zone:hover { border-color: var(--gold); color: var(--gold); }

@keyframes float {
  0% { transform: translateY(0) rotate(0deg); opacity: 1; }
  100% { transform: translateY(-100px) rotate(360deg); opacity: 0; }
}

/* ROTATION HANDLE — Canva-style */
.rotate-handle {
  position: absolute;
  bottom: -28px;
  left: 50%;
  transform: translateX(-50%);
  width: 18px;
  height: 18px;
  background: #fff;
  border: 2px solid var(--gold);
  border-radius: 50%;
  cursor: grab;
  display: none;
  align-items: center;
  justify-content: center;
  font-size: 10px;
  z-index: 30;
  user-select: none;
  touch-action: none;
}
.rotate-handle::after {
  content: '↻';
  color: var(--gold-dark);
  font-size: 11px;
  line-height: 1;
}
/* Line connecting sticker to handle */
.rotate-handle::before {
  content: '';
  position: absolute;
  top: -10px;
  left: 50%;
  transform: translateX(-50%);
  width: 2px;
  height: 10px;
  background: var(--gold);
}
.letter-sticker.selected .rotate-handle { display: flex; }
.rotate-handle:active { cursor: grabbing; }

/* ROTATION SLIDER */
.rotation-wrap { display: flex; align-items: center; gap: 8px; margin-top: 6px; }
.rotation-val { font-size: 9px; color: var(--gold); min-width: 40px; text-align: right; }

.canvas-info { font-family: 'VT323', monospace; font-size: 14px; color: #888; padding: 4px 8px; }

/* EMOJI PASTE ZONE */
.emoji-paste-area { background: var(--ui-dark); border: 2px solid var(--ui-light); padding: 6px; width: 100%; font-size: 22px; text-align: center; color: #fff; letter-spacing: 4px; min-height: 42px; cursor: text; outline: none; caret-color: var(--gold); font-family: sans-serif; }
.emoji-paste-area:focus { border-color: var(--gold); }

/* TEXT TOOL / SELECT TOOL indicator */
.tool-mode-bar { display: flex; gap: 4px; margin-bottom: 8px; }
.tool-mode-btn { flex: 1; font-family: 'Press Start 2P', monospace; font-size: 7px; padding: 5px; cursor: pointer; background: var(--ui-mid); color: #aaa; border: 2px solid #000; text-align: center; }
.tool-mode-btn.active { background: var(--gold-dark); color: #1a0f05; }

/* GRADIENT DIRECTION */
.grad-dir-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 3px; margin-bottom: 6px; }
.grad-dir-btn { font-size: 14px; padding: 5px; cursor: pointer; background: var(--ui-mid); border: 2px solid transparent; text-align: center; }
.grad-dir-btn:hover, .grad-dir-btn.active { border-color: var(--gold); background: var(--ui-light); }

/* sticker search */
.sticker-search { width: 100%; padding: 5px 8px; font-family: 'VT323', monospace; font-size: 16px; background: var(--ui-dark); color: #e0d4b8; border: 2px solid var(--ui-light); margin-bottom: 6px; outline: none; }
.sticker-search:focus { border-color: var(--gold); }

/* canvas scroll wrapper */
.canvas-scroll { overflow: auto; max-height: 75vh; }
</style>
</head>
<body>

<header>
  <div class="logo">✉ PixelLetter ✉</div>
  <div class="tagline">► Craft magical pixel letters & share them with the world!❣️ ◄</div>
</header>

<div class="app">
  <!-- SIDEBAR -->
  <div class="sidebar">

    <!-- TABS -->
    <div class="sidebar-tabs">
      <button class="side-tab active" onclick="switchTab('text')">✏TEXT</button>
      <button class="side-tab" onclick="switchTab('sticker')">🎨ART</button>
      <button class="side-tab" onclick="switchTab('bg')">🖼THEME</button>
      <button class="side-tab" onclick="switchTab('canvas')">📐SIZE</button>
    </div>

    <!-- TEXT TAB -->
    <div id="tab-text" class="tool-section px-panel">
      <div class="section-title">◆ TEXT COLOR</div>
      <div style="font-family:'VT323',monospace;font-size:13px;color:#a0d8a0;margin-bottom:6px">💡 Select text first, then pick color/font/size to style just that block</div>
      <div class="color-grid" id="colorGrid"></div>
      <span class="px-label">Custom color:</span>
      <input type="color" id="customColor" value="#2d1e0f" style="width:100%;height:28px;border:2px solid #000;cursor:pointer;padding:2px;background:var(--ui-mid)" onmousedown="saveSelection()" onchange="setColor(this.value)">

      <div style="margin-top:12px">
        <div class="section-title">◆ FONT FACE</div>
        <div class="font-grid" id="fontGrid"></div>
      </div>

      <div style="margin-top:12px">
        <div class="section-title">◆ FONT SIZE</div>
        <input type="range" class="px-slider" id="fontSize" min="8" max="96" value="16" onmousedown="saveSelection()" oninput="setSize(this.value)">
        <div class="size-display" id="sizeDisplay">16px</div>
      </div>

      <div style="margin-top:12px">
        <div class="section-title">◆ TEXT STYLE</div>
        <div style="display:flex;gap:6px;flex-wrap:wrap">
          <button class="px-btn sm" onmousedown="event.preventDefault();saveSelection()" onclick="execCmd('bold')" title="Bold"><b>B</b></button>
          <button class="px-btn sm" onmousedown="event.preventDefault();saveSelection()" onclick="execCmd('italic')" title="Italic"><i>I</i></button>
          <button class="px-btn sm" onmousedown="event.preventDefault();saveSelection()" onclick="execCmd('underline')" title="Underline"><u>U</u></button>
          <button class="px-btn sm" onmousedown="event.preventDefault();saveSelection()" onclick="execCmd('strikeThrough')" title="Strike"><s>S</s></button>
          <button class="px-btn sm blue" onmousedown="event.preventDefault();saveSelection()" onclick="execCmd('justifyLeft')">◁</button>
          <button class="px-btn sm blue" onmousedown="event.preventDefault();saveSelection()" onclick="execCmd('justifyCenter')">◈</button>
          <button class="px-btn sm blue" onmousedown="event.preventDefault();saveSelection()" onclick="execCmd('justifyRight')">▷</button>
        </div>
      </div>
    </div>

    <!-- STICKER TAB -->
    <div id="tab-sticker" class="tool-section px-panel" style="display:none">
      <!-- PASTE/TYPE EMOJI BOX -->
      <div class="section-title">◆ PASTE EMOJI / TEXT</div>
      <div style="display:flex;gap:4px;margin-bottom:8px">
        <div class="emoji-paste-area" id="emojiPasteArea" contenteditable="true" spellcheck="false" placeholder="Paste emoji here..."></div>
        <button class="px-btn" style="font-size:8px;padding:5px 8px;white-space:nowrap" onclick="addStickerFromPaste()">ADD ▶</button>
      </div>
      <span class="px-label">Type, paste or drag any emoji/text above then click ADD</span>

      <div class="section-title" style="margin-top:10px">◆ SEARCH STICKERS</div>
      <input class="sticker-search" id="stickerSearch" placeholder="Search emoji..." oninput="filterStickers(this.value)">

      <div id="stickerCats">
        <div class="section-title">❤ HEARTS</div>
        <div class="sticker-grid" id="stickers-hearts"></div>
        <div class="section-title" style="margin-top:8px">🎀 DECORATIONS</div>
        <div class="sticker-grid" id="stickers-deco"></div>
        <div class="section-title" style="margin-top:8px">🐱 CUTE</div>
        <div class="sticker-grid" id="stickers-cute"></div>
        <div class="section-title" style="margin-top:8px">⚔ PIXEL</div>
        <div class="sticker-grid" id="stickers-pixel"></div>
        <div class="section-title" style="margin-top:8px">😂 FACES</div>
        <div class="sticker-grid" id="stickers-faces"></div>
        <div class="section-title" style="margin-top:8px">🍕 FOOD</div>
        <div class="sticker-grid" id="stickers-food"></div>
        <div class="section-title" style="margin-top:8px">🌍 NATURE</div>
        <div class="sticker-grid" id="stickers-nature"></div>
        <div class="section-title" style="margin-top:8px">✨ SYMBOLS</div>
        <div class="sticker-grid" id="stickers-symbols"></div>
      </div>
      <div id="stickerSearchResults" style="display:none">
        <div class="section-title">🔍 RESULTS</div>
        <div class="sticker-grid" id="stickers-search"></div>
      </div>

      <div style="margin-top:10px">
        <div class="section-title">◆ ADD IMAGE STICKER</div>
        <div class="upload-zone" onclick="document.getElementById('imgUpload').click()" style="margin-bottom:4px">📁 Click to upload image</div>
        <div class="upload-zone" style="cursor:default;border-style:solid;border-color:var(--gold);color:var(--gold);font-size:7px;padding:8px">
          ⌘V / Ctrl+V — paste image anywhere<br>or drag &amp; drop onto the canvas
        </div>
        <input type="file" id="imgUpload" accept="image/*" style="display:none" onchange="addImage(event)">
        <input type="text" id="imgUrl" placeholder="...or paste image URL" style="width:100%;margin-top:6px;padding:6px;font-family:'VT323',monospace;font-size:14px;background:var(--ui-dark);color:#e0d4b8;border:2px solid var(--ui-light);outline:none" onkeydown="if(event.key==='Enter')addImageUrl()">
        <button class="px-btn sm" style="margin-top:4px;width:100%" onclick="addImageUrl()">ADD IMAGE ▶</button>
      </div>

      <div style="margin-top:10px">
        <div class="section-title">◆ STICKER SIZE</div>
        <input type="range" class="px-slider" id="stickerSize" min="16" max="400" value="36" oninput="resizeStickerSelected(this.value)">
        <div class="size-display" id="stickerSizeDisplay">36px</div>
      </div>

      <div style="margin-top:10px">
        <div class="section-title">◆ STICKER ROTATION</div>
        <div class="rotation-wrap">
          <input type="range" class="px-slider" id="stickerRotation" min="0" max="360" value="0" style="flex:1" oninput="rotateSelected(this.value)">
          <span class="rotation-val" id="rotationVal">0°</span>
        </div>
        <div style="display:flex;gap:4px;margin-top:6px;flex-wrap:wrap">
          <button class="px-btn sm" onclick="rotateTo(0)">0°</button>
          <button class="px-btn sm" onclick="rotateTo(45)">45°</button>
          <button class="px-btn sm" onclick="rotateTo(90)">90°</button>
          <button class="px-btn sm" onclick="rotateTo(180)">180°</button>
          <button class="px-btn sm" onclick="rotateTo(-45)">-45°</button>
          <button class="px-btn sm red" onclick="flipSelected()">↔ FLIP</button>
        </div>
        <span class="px-label" style="margin-top:4px">Select a sticker → drag the ↻ handle below it to rotate freely. Hold Shift to snap 45°.</span>
      </div>
    </div>

    <!-- BG/THEME TAB -->
    <div id="tab-bg" class="tool-section px-panel" style="display:none">
      <div class="section-title">◆ CANVAS THEME</div>
      <div class="theme-tabs" id="themeGrid"></div>

      <div style="margin-top:10px">
        <div class="section-title">◆ CUSTOM BG COLOR</div>
        <div class="bg-color-grid" id="bgColorGrid"></div>
        <span class="px-label">Custom BG color:</span>
        <input type="color" id="customBgColor" value="#faf3dc" style="width:100%;height:28px;border:2px solid #000;cursor:pointer;padding:2px;background:var(--ui-mid)" onchange="setBgColor(this.value)">
      </div>

      <div style="margin-top:10px">
        <div class="section-title">◆ GRADIENT BG</div>
        <span class="px-label">Direction:</span>
        <div class="grad-dir-grid" id="gradDirGrid">
          <div class="grad-dir-btn active" data-dir="to bottom" onclick="setGradDir(this,'to bottom')">↓</div>
          <div class="grad-dir-btn" data-dir="to right" onclick="setGradDir(this,'to right')">→</div>
          <div class="grad-dir-btn" data-dir="135deg" onclick="setGradDir(this,'135deg')">↘</div>
          <div class="grad-dir-btn" data-dir="45deg" onclick="setGradDir(this,'45deg')">↗</div>
        </div>
        <span class="px-label">Color 1:</span>
        <input type="color" id="gradColor1" value="#faf3dc" style="width:48%;height:28px;border:2px solid #000;cursor:pointer;padding:2px;background:var(--ui-mid)" oninput="applyGradient()">
        <input type="color" id="gradColor2" value="#f9c22b" style="width:48%;height:28px;border:2px solid #000;cursor:pointer;padding:2px;background:var(--ui-mid);margin-left:2%" oninput="applyGradient()">
        <button class="px-btn sm" style="margin-top:4px;width:100%" onclick="applyGradient()">APPLY GRADIENT ▶</button>
        <button class="px-btn sm red" style="margin-top:4px;width:100%" onclick="clearGradient()">CLEAR GRADIENT</button>
      </div>

      <div style="margin-top:10px">
        <div class="section-title">◆ PAPER TEXTURE</div>
        <div style="display:flex;flex-wrap:wrap;gap:4px">
          <button class="px-btn sm" onclick="setBgPattern('none')">PLAIN</button>
          <button class="px-btn sm" onclick="setBgPattern('lines')">LINED</button>
          <button class="px-btn sm" onclick="setBgPattern('dots')">DOTTED</button>
          <button class="px-btn sm" onclick="setBgPattern('grid')">GRID</button>
          <button class="px-btn sm" onclick="setBgPattern('checker')">CHECK</button>
          <button class="px-btn sm" onclick="setBgPattern('diagonal')">DIAG</button>
          <button class="px-btn sm" onclick="setBgPattern('hex')">HEX</button>
        </div>
      </div>

      <div style="margin-top:10px">
        <div class="section-title">◆ BORDER STYLE</div>
        <div style="display:flex;flex-wrap:wrap;gap:4px">
          <button class="px-btn sm" onclick="setBorder('classic')">CLASSIC</button>
          <button class="px-btn sm gold" onclick="setBorder('gold')">GOLDEN</button>
          <button class="px-btn sm blue" onclick="setBorder('diamond')">DIAMOND</button>
          <button class="px-btn sm red" onclick="setBorder('redstone')">REDSTONE</button>
          <button class="px-btn sm purple" onclick="setBorder('enchant')">ENCHANT</button>
          <button class="px-btn sm" onclick="setBorder('dashed')">DASHED</button>
          <button class="px-btn sm" onclick="setBorder('double')">DOUBLE</button>
          <button class="px-btn sm" onclick="setBorder('none')">NONE</button>
        </div>
      </div>

      <div style="margin-top:10px">
        <div class="section-title">◆ BG IMAGE</div>
        <div class="upload-zone" onclick="document.getElementById('bgUpload').click()">Set background image 🖼</div>
        <input type="file" id="bgUpload" accept="image/*" style="display:none" onchange="setBgImage(event)">
        <button class="px-btn sm red" style="margin-top:4px;width:100%" onclick="clearBgImage()">CLEAR BG IMAGE</button>
      </div>
    </div>

    <!-- CANVAS SIZE TAB -->
    <div id="tab-canvas" class="tool-section px-panel" style="display:none">
      <div class="section-title">◆ CANVAS RATIO</div>
      <div class="ratio-grid" id="ratioGrid"></div>

      <div style="margin-top:10px">
        <div class="section-title">◆ CUSTOM SIZE</div>
        <span class="px-label">Width (px):</span>
        <input type="number" id="customW" value="560" min="200" max="1200" style="width:100%;padding:5px;font-family:'VT323',monospace;font-size:16px;background:var(--ui-dark);color:#e0d4b8;border:2px solid var(--ui-light);outline:none;margin-bottom:6px">
        <span class="px-label">Height (px):</span>
        <input type="number" id="customH" value="460" min="200" max="1200" style="width:100%;padding:5px;font-family:'VT323',monospace;font-size:16px;background:var(--ui-dark);color:#e0d4b8;border:2px solid var(--ui-light);outline:none;margin-bottom:6px">
        <button class="px-btn" style="width:100%;font-size:8px" onclick="applyCustomSize()">APPLY SIZE ▶</button>
      </div>

      <div style="margin-top:10px" class="canvas-info" id="canvasInfo">Canvas: 560 × 460 px</div>
    </div>

    <!-- ACTION BUTTONS -->
    <div class="px-panel-dark" style="padding:10px;display:flex;flex-direction:column;gap:6px">
      <button class="px-btn gold lg" onclick="showPreview()" style="width:100%">▶ PREVIEW LETTER</button>
      <button class="px-btn blue" id="generateLinkBtn" onclick="generateLink()" style="width:100%">🔗 GENERATE SHARE LINK</button>
      <button class="px-btn sm red" onclick="clearAll()" style="width:100%">🗑 CLEAR ALL</button>
    </div>

  </div>

  <!-- LETTER CANVAS AREA -->
  <div class="letter-area">
    <div class="px-panel-dark editor-toolbar">
      <span style="font-size:7px;color:#888">TO:</span>
      <input type="text" id="toField" placeholder="Recipient name..." style="font-family:'VT323',monospace;font-size:16px;background:transparent;border:none;border-bottom:2px solid var(--ui-light);color:#e0d4b8;padding:2px 6px;width:140px;outline:none">
      <div class="toolbar-sep"></div>
      <span style="font-size:7px;color:#888">FROM:</span>
      <input type="text" id="fromField" placeholder="Your name..." style="font-family:'VT323',monospace;font-size:16px;background:transparent;border:none;border-bottom:2px solid var(--ui-light);color:#e0d4b8;padding:2px 6px;width:110px;outline:none">
      <div class="toolbar-sep"></div>
      <!-- Tool mode buttons -->
      <button class="px-btn sm active-tool" id="toolText" onclick="setToolMode('text')" title="Text mode">✏ TEXT</button>
      <button class="px-btn sm" id="toolSelect" onclick="setToolMode('select')" title="Select stickers">↖ SELECT</button>
      <div class="toolbar-sep"></div>
      <button class="px-btn sm" onclick="addSparkle()">✨</button>
      <button class="px-btn sm gold" onclick="addParticles()">🎊</button>
    </div>

    <div class="canvas-scroll">
      <div class="canvas-wrapper px-panel" id="canvasWrapper" style="padding:8px;width:fit-content">
        <div class="letter-paper pixel-border theme-default" id="letterPaper" style="width:560px;height:460px">
          <div class="text-content" id="textContent" contenteditable="true" spellcheck="true"
            style="font-size:16px;color:#2d1e0f;font-family:'Press Start 2P',monospace"
            onmouseup="saveSelection()"
            onkeyup="saveSelection()"
            onselect="saveSelection()">Write your magical letter here... ✨</div>
        </div>
      </div>
    </div>

    <div class="px-panel-dark" style="padding:8px;font-size:7px;color:#888;text-align:center">
      CLICK PAPER TO EDIT TEXT · CLICK STICKER TO SELECT · DRAG TO REPOSITION · ⌘V/CTRL+V TO PASTE IMAGE AS STICKER
    </div>
  </div>
</div>

<!-- PREVIEW MODAL -->
<div class="modal-overlay" id="previewModal" onclick="if(event.target===this)closeModal()">
  <div class="modal-box" id="previewContent">
    <div style="text-align:right;margin-bottom:12px">
      <button class="px-btn sm red" onclick="closeModal()">✕ CLOSE</button>
    </div>
    <div id="previewLetter"></div>
    <div style="margin-top:16px">
      <div style="font-family:'Press Start 2P',monospace;font-size:8px;color:#5aad5a;margin-bottom:8px">🔗 SHAREABLE LINK — works on any device:</div>
      <div class="share-box" id="shareBox" onclick="copyLink()">Generate link first...</div>
    </div>
  </div>
</div>

<div class="notif" id="notif" style="display:none"></div>

<script>
// ═══════════════════════════════════════════
//  DATA
// ═══════════════════════════════════════════
const COLORS = [
  '#000000','#ffffff','#ff0000','#00aa00','#0000ff','#ffff00','#ff00ff','#00ffff',
  '#2d1e0f','#8b1a1a','#1a4a8b','#1a8b3a','#8b6b1a','#5a1a8b','#1a8b8b','#8b1a6b',
  '#ff4444','#4488ff','#44cc66','#ffcc22','#cc44ff','#44ddcc','#ff88cc','#ffaa44',
  '#5aad5a','#f9c22b','#5de8f0','#e83232','#c08b5c','#866043','#6ec1e4','#fff9c4',
  '#ff6b6b','#feca57','#48dbfb','#ff9ff3','#54a0ff','#5f27cd','#00d2d3','#1dd1a1',
  '#ee5a24','#009432','#0652DD','#9980FA','#833471','#006266','#1B1464','#B53471',
];

const BG_COLORS = [
  '#ffffff','#faf3dc','#f5e6c8','#e8f5e9','#fff0f5','#e8d5f5','#d4f5e9','#e0f7fa',
  '#0d0730','#0a0020','#1a0000','#1a3d1a','#0a0a2e','#1a0a30','#0a0520','#1a0f05',
  '#ffb3c6','#ffd6a5','#caffbf','#fdffb6','#a0c4ff','#bdb2ff','#ffc6ff','#d0f4de',
  '#ff6b6b','#feca57','#ff9f43','#54a0ff','#5f27cd','#341f97','#2c3e50','#34495e',
];

const FONTS = [
  { name:'PIXEL (Press Start)', css:"'Press Start 2P', monospace" },
  { name:'VT323 Terminal', css:"'VT323', monospace" },
  { name:'Silkscreen', css:"'Silkscreen', monospace" },
  { name:'Pixelify Sans', css:"'Pixelify Sans', monospace" },
  { name:'Orbitron Sci-Fi', css:"'Orbitron', sans-serif" },
  { name:'Space Mono', css:"'Space Mono', monospace" },
  { name:'Courier Prime', css:"'Courier Prime', monospace" },
  { name:'Josefin Sans', css:"'Josefin Sans', sans-serif" },
  { name:'Playfair Display', css:"'Playfair Display', serif" },
  { name:'Abril Fatface', css:"'Abril Fatface', serif" },
  { name:'Permanent Marker', css:"'Permanent Marker', cursive" },
  { name:'Caveat Handwriting', css:"'Caveat', cursive" },
  { name:'Dancing Script', css:"'Dancing Script', cursive" },
  { name:'Creepster Horror', css:"'Creepster', cursive" },
  { name:'Righteous', css:"'Righteous', cursive" },
  { name:'Georgia Serif', css:"Georgia, serif" },
  { name:'Courier Mono', css:"'Courier New', monospace" },
];

const STICKERS = {
  hearts: ['❤️','🧡','💛','💚','💙','💜','🖤','🤍','💗','💓','💞','💕','💖','💝','💘','💟','♥','❣️','💑','💏'],
  deco: ['🎀','🎁','🎊','🎉','🎈','🌟','⭐','✨','🌙','☀️','🌈','🦋','🌸','🌺','🌻','🍀','🌷','🌹','🪷','🌼'],
  cute: ['🐱','🐶','🐰','🦊','🐼','🐨','🦄','🐸','🐙','🦋','🐝','🦜','🐢','🐞','🦆','🐣','🐥','🐟','🐬','🦁'],
  pixel: ['⚔️','🗡️','🏹','🛡️','💎','💰','🏆','👑','🔮','⚡','🌀','❄️','🔥','💫','⚙️','🧩','🎮','🕹️','🎲','🎯'],
  faces: ['😀','😂','🥹','😍','🤩','😎','🥸','🤔','😴','🤯','🥳','😭','😱','🤗','🫡','🥺','😏','🤭','😈','👻'],
  food: ['🍕','🍔','🍟','🌮','🌯','🍜','🍣','🍩','🍰','🎂','🍫','🍬','🧁','🍪','🥐','🍦','🧃','🥤','☕','🧋'],
  nature: ['🌊','🏔️','🌋','🌅','🌄','🌃','🏖️','🌴','🍄','🌿','🍃','🌱','🌾','🎋','🎍','🌏','🪐','🌌','☄️','🌠'],
  symbols: ['♠️','♣️','♥️','♦️','🔴','🟠','🟡','🟢','🔵','🟣','⚫','⚪','🟤','🔶','🔷','🔸','🔹','🔺','🔻','▪️'],
};

const THEMES = [
  { id:'default', name:'CLASSIC', class:'theme-default', preview:'#faf3dc' },
  { id:'night', name:'NIGHT SKY', class:'theme-night', preview:'#0d0730' },
  { id:'marvel', name:'MARVEL', class:'theme-marvel', preview:'#8b0000' },
  { id:'nature', name:'NATURE', class:'theme-nature', preview:'#e8f5e9' },
  { id:'ocean', name:'OCEAN', class:'theme-ocean', preview:'#0077b6' },
  { id:'candy', name:'CANDY', class:'theme-candy', preview:'#ffb3c6' },
  { id:'galaxy', name:'GALAXY', class:'theme-galaxy', preview:'#0a0020' },
  { id:'retro', name:'RETRO', class:'theme-retro', preview:'#1a0000' },
  { id:'pastel', name:'PASTEL', class:'theme-pastel', preview:'#fff0f5' },
  { id:'parchment', name:'SCROLL', class:'theme-parchment', preview:'#f5e6c8' },
  { id:'sunset', name:'SUNSET', class:'theme-sunset', preview:'#ff6b6b' },
  { id:'mint', name:'MINT', class:'theme-mint', preview:'#d4f5e9' },
  { id:'lavender', name:'LAVENDER', class:'theme-lavender', preview:'#e8d5f5' },
  { id:'midnight', name:'MIDNIGHT', class:'theme-midnight', preview:'#0a0a2e' },
  { id:'forest', name:'FOREST', class:'theme-forest', preview:'#1a3d1a' },
  { id:'rose', name:'ROSE', class:'theme-rose', preview:'#fce4ec' },
  { id:'arctic', name:'ARCTIC', class:'theme-arctic', preview:'#e0f7fa' },
  { id:'lava', name:'LAVA', class:'theme-lava', preview:'#1a0000' },
];

const RATIOS = [
  { id:'letter', name:'LETTER', sub:'560×460', w:560, h:460 },
  { id:'square', name:'SQUARE', sub:'500×500', w:500, h:500 },
  { id:'portrait', name:'PORTRAIT', sub:'400×560', w:400, h:560 },
  { id:'landscape', name:'LANDSCAPE', sub:'700×400', w:700, h:400 },
  { id:'story', name:'STORY', sub:'400×710', w:400, h:710 },
  { id:'wide', name:'WIDE', sub:'800×450', w:800, h:450 },
  { id:'card', name:'CARD', sub:'500×320', w:500, h:320 },
  { id:'banner', name:'BANNER', sub:'900×300', w:900, h:300 },
];

// ═══════════════════════════════════════════
//  STATE
// ═══════════════════════════════════════════
let currentColor = '#2d1e0f';
let currentFont = FONTS[0].css;
let currentSize = 16;
let currentTheme = 'default';
let bgPattern = 'none';
let borderStyle = 'classic';
let shareLink = '';
let draggingEl = null;
let dragOffX = 0, dragOffY = 0;
let selectedSticker = null;
let currentRotation = 0;
let currentFlip = false;
let gradDir = 'to bottom';
let toolMode = 'text'; // 'text' | 'select'
let bgColorOverride = null;
let gradientActive = false;

// ═══════════════════════════════════════════
//  INIT
// ═══════════════════════════════════════════
function init() {
  // Color grid
  const cg = document.getElementById('colorGrid');
  COLORS.forEach(c => {
    const s = document.createElement('div');
    s.className = 'color-swatch';
    s.style.background = c;
    s.title = c;
    s.onmousedown = (e) => { e.preventDefault(); saveSelection(); };
    s.onclick = () => setColor(c);
    if (c === currentColor) s.classList.add('selected');
    cg.appendChild(s);
  });

  // BG Color grid
  const bcg = document.getElementById('bgColorGrid');
  BG_COLORS.forEach(c => {
    const s = document.createElement('div');
    s.className = 'bg-color-swatch';
    s.style.background = c;
    s.title = c;
    s.onclick = () => setBgColor(c);
    bcg.appendChild(s);
  });

  // Font grid
  const fg = document.getElementById('fontGrid');
  FONTS.forEach((f, i) => {
    const b = document.createElement('div');
    b.className = 'font-opt' + (i===0?' selected':'');
    b.style.fontFamily = f.css;
    b.textContent = f.name;
    b.onmousedown = (e) => { e.preventDefault(); saveSelection(); };
    b.onclick = () => {
      setFont(f.css);
      document.querySelectorAll('.font-opt').forEach(x => x.classList.remove('selected'));
      b.classList.add('selected');
    };
    fg.appendChild(b);
  });

  // Sticker grids
  Object.entries(STICKERS).forEach(([cat, emojis]) => {
    const g = document.getElementById('stickers-'+cat);
    if (!g) return;
    emojis.forEach(e => {
      const b = document.createElement('div');
      b.className = 'sticker-btn';
      b.textContent = e;
      b.onclick = () => addSticker(e);
      g.appendChild(b);
    });
  });

  // Themes
  const tg = document.getElementById('themeGrid');
  THEMES.forEach(t => {
    const b = document.createElement('button');
    b.className = 'theme-tab' + (t.id==='default'?' active':'');
    b.style.borderLeft = `4px solid ${t.preview}`;
    b.textContent = t.name;
    b.onclick = () => {
      setTheme(t.id, t.class);
      document.querySelectorAll('.theme-tab').forEach(x=>x.classList.remove('active'));
      b.classList.add('active');
    };
    tg.appendChild(b);
  });

  // Ratios
  const rg = document.getElementById('ratioGrid');
  RATIOS.forEach((r, i) => {
    const b = document.createElement('button');
    b.className = 'ratio-btn' + (i===0?' active':'');
    b.innerHTML = `${r.name}<br><span style="font-size:5px;color:#aaa">${r.sub}</span>`;
    b.onclick = () => {
      setCanvasSize(r.w, r.h);
      document.querySelectorAll('.ratio-btn').forEach(x=>x.classList.remove('active'));
      b.classList.add('active');
    };
    rg.appendChild(b);
  });

  // Drag
  document.addEventListener('mousemove', onDragMove);
  document.addEventListener('mouseup', onDragEnd);
  document.addEventListener('touchmove', onDragMove, {passive:false});
  document.addEventListener('touchend', onDragEnd);

  // Click outside to deselect
  document.getElementById('letterPaper').addEventListener('mousedown', (e) => {
    if (e.target === document.getElementById('letterPaper') || e.target === document.getElementById('textContent')) {
      deselectSticker();
      deselectImgSticker();
    }
  });

  // Keyboard shortcuts for selected sticker
  document.addEventListener('keydown', (e) => {
    if (!selectedSticker) return;
    const step = e.shiftKey ? 10 : 2;
    if (e.key === 'ArrowLeft') { selectedSticker.style.left = (parseFloat(selectedSticker.style.left)||0) - step + 'px'; e.preventDefault(); }
    if (e.key === 'ArrowRight') { selectedSticker.style.left = (parseFloat(selectedSticker.style.left)||0) + step + 'px'; e.preventDefault(); }
    if (e.key === 'ArrowUp') { selectedSticker.style.top = (parseFloat(selectedSticker.style.top)||0) - step + 'px'; e.preventDefault(); }
    if (e.key === 'ArrowDown') { selectedSticker.style.top = (parseFloat(selectedSticker.style.top)||0) + step + 'px'; e.preventDefault(); }
    if (e.key === 'Delete' || e.key === 'Backspace') { selectedSticker.remove(); selectedSticker = null; }
  });

  // ── PASTE IMAGE FROM CLIPBOARD → place as sticker ──
  document.addEventListener('paste', (e) => {
    const items = e.clipboardData && e.clipboardData.items;
    if (!items) return;
    for (let i = 0; i < items.length; i++) {
      if (items[i].type.startsWith('image/')) {
        e.preventDefault();
        const blob = items[i].getAsFile();
        const reader = new FileReader();
        reader.onload = ev => placeImage(ev.target.result);
        reader.readAsDataURL(blob);
        switchTab('sticker');
        showNotif('Image pasted as sticker! 🎨');
        return;
      }
    }
  });

  // ── DRAG-AND-DROP IMAGE ONTO CANVAS → place as sticker ──
  const paperEl = document.getElementById('letterPaper');
  paperEl.addEventListener('dragover', (e) => { e.preventDefault(); e.dataTransfer.dropEffect = 'copy'; });
  paperEl.addEventListener('drop', (e) => {
    e.preventDefault();
    const files = e.dataTransfer.files;
    if (files && files[0] && files[0].type.startsWith('image/')) {
      const reader = new FileReader();
      reader.onload = ev => placeImage(ev.target.result);
      reader.readAsDataURL(files[0]);
      switchTab('sticker');
      showNotif('Image dropped as sticker! 🎨');
    }
  });
}

// ═══════════════════════════════════════════
//  TOOL MODE
// ═══════════════════════════════════════════
function setToolMode(mode) {
  toolMode = mode;
  const paper = document.getElementById('letterPaper');
  const textContent = document.getElementById('textContent');
  document.getElementById('toolText').classList.toggle('active-tool', mode==='text');
  document.getElementById('toolSelect').classList.toggle('active-tool', mode==='select');
  if (mode === 'text') {
    paper.style.cursor = 'text';
    textContent.contentEditable = 'true';
  } else {
    paper.style.cursor = 'default';
    textContent.contentEditable = 'false';
  }
}

// ═══════════════════════════════════════════
//  STICKER SELECTION
// ═══════════════════════════════════════════
function selectSticker(el) {
  deselectSticker();
  selectedSticker = el;
  el.classList.add('selected');
  // sync rotation slider
  const rot = parseInt(el.dataset.rotation || '0');
  document.getElementById('stickerRotation').value = rot;
  document.getElementById('rotationVal').textContent = rot + '°';
  // sync size slider — emoji sticker uses span fontSize, image sticker uses img width
  const span = el.querySelector('span');
  const img  = el.querySelector('img');
  if (span) {
    const sz = parseInt(span.style.fontSize) || 36;
    document.getElementById('stickerSize').value = Math.min(sz, 400);
    document.getElementById('stickerSizeDisplay').textContent = sz + 'px';
  } else if (img) {
    const sz = parseInt(img.style.width) || 80;
    document.getElementById('stickerSize').value = Math.min(sz, 400);
    document.getElementById('stickerSizeDisplay').textContent = sz + 'px';
  }
  switchTab('sticker');
}

function deselectSticker() {
  if (selectedSticker) {
    selectedSticker.classList.remove('selected');
    selectedSticker = null;
  }
}

// ═══════════════════════════════════════════
//  STICKER ROTATION / FLIP
// ═══════════════════════════════════════════
function rotateSelected(deg) {
  document.getElementById('rotationVal').textContent = deg + '°';
  if (!selectedSticker) return;
  selectedSticker.dataset.rotation = deg;
  applyTransform(selectedSticker);
}

function resizeStickerSelected(size) {
  document.getElementById('stickerSizeDisplay').textContent = size + 'px';
  // emoji sticker
  if (selectedSticker) {
    const span = selectedSticker.querySelector('span');
    if (span) span.style.fontSize = size + 'px';
  }
  // image sticker
  if (selectedImgSticker) {
    const img = selectedImgSticker.querySelector('img');
    if (img) {
      const aspect = (img.naturalHeight || img.offsetHeight || 1) / (img.naturalWidth || img.offsetWidth || 1);
      img.style.width  = size + 'px';
      img.style.height = (size * aspect) + 'px';
    }
  }
}

function rotateTo(deg) {
  if (deg < 0) deg = 360 + deg;
  document.getElementById('stickerRotation').value = deg;
  rotateSelected(deg);
}

function flipSelected() {
  if (!selectedSticker) { showNotif('Select a sticker first!', true); return; }
  const current = selectedSticker.dataset.flip === '1';
  selectedSticker.dataset.flip = current ? '0' : '1';
  applyTransform(selectedSticker);
}

function applyTransform(el) {
  const deg = el.dataset.rotation || '0';
  const flip = el.dataset.flip === '1' ? ' scaleX(-1)' : '';
  el.style.transform = `rotate(${deg}deg)${flip}`;
}

// ═══════════════════════════════════════════
//  STICKER FROM PASTE
// ═══════════════════════════════════════════
function addStickerFromPaste() {
  const area = document.getElementById('emojiPasteArea');
  const text = area.innerText.trim();
  if (!text) { showNotif('Type or paste something first!', true); return; }
  addSticker(text);
  area.innerText = '';
  showNotif('Sticker added! 🎉');
}

// ═══════════════════════════════════════════
//  STICKER SEARCH
// ═══════════════════════════════════════════
function filterStickers(query) {
  const cats = document.getElementById('stickerCats');
  const results = document.getElementById('stickerSearchResults');
  const grid = document.getElementById('stickers-search');
  if (!query.trim()) {
    cats.style.display = '';
    results.style.display = 'none';
    return;
  }
  cats.style.display = 'none';
  results.style.display = '';
  grid.innerHTML = '';
  const all = Object.values(STICKERS).flat();
  // simple filter - just show all if no match logic
  all.forEach(e => {
    const b = document.createElement('div');
    b.className = 'sticker-btn';
    b.textContent = e;
    b.onclick = () => addSticker(e);
    grid.appendChild(b);
  });
}

// ═══════════════════════════════════════════
//  COLOR / FONT / SIZE
// ═══════════════════════════════════════════
// ═══════════════════════════════════════════
//  SAVED SELECTION — so clicks on sidebar don't lose caret/selection
// ═══════════════════════════════════════════
let savedRange = null;

function saveSelection() {
  const tc = document.getElementById('textContent');
  const sel = window.getSelection();
  if (sel && sel.rangeCount > 0 && tc.contains(sel.anchorNode)) {
    savedRange = sel.getRangeAt(0).cloneRange();
  }
}

function restoreSelection() {
  const tc = document.getElementById('textContent');
  tc.focus();
  if (savedRange) {
    const sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(savedRange);
  }
}

function applyStyleToSelection(styleProp, styleVal) {
  restoreSelection();
  const tc = document.getElementById('textContent');
  const sel = window.getSelection();
  if (!sel || sel.rangeCount === 0) return;

  const range = sel.getRangeAt(0);

  if (!sel.isCollapsed) {
    // Wrap selected text in a styled span
    const span = document.createElement('span');
    span.style[styleProp] = styleVal;
    try {
      range.surroundContents(span);
    } catch(e) {
      // Selection crosses element boundaries — extract and rewrap
      const frag = range.extractContents();
      span.appendChild(frag);
      range.insertNode(span);
    }
    // Move caret to end of inserted span
    const newRange = document.createRange();
    newRange.setStartAfter(span);
    newRange.collapse(true);
    sel.removeAllRanges();
    sel.addRange(newRange);
    savedRange = newRange.cloneRange();
  } else {
    // No selection — insert a zero-width marker span so next typed chars inherit the style
    const span = document.createElement('span');
    span.style[styleProp] = styleVal;
    span.appendChild(document.createTextNode('\u200B')); // zero-width space as placeholder
    range.insertNode(span);
    // Place caret inside the span (after zero-width char)
    const newRange = document.createRange();
    newRange.setStart(span.firstChild, 1);
    newRange.collapse(true);
    sel.removeAllRanges();
    sel.addRange(newRange);
    savedRange = newRange.cloneRange();
  }
}

function setColor(c) {
  currentColor = c;
  document.querySelectorAll('.color-swatch').forEach(s => {
    s.classList.toggle('selected', s.title === c);
  });
  document.getElementById('customColor').value = c;
  applyStyleToSelection('color', c);
}

function setFont(f) {
  currentFont = f;
  applyStyleToSelection('fontFamily', f);
}

function setSize(s) {
  currentSize = parseInt(s);
  document.getElementById('sizeDisplay').textContent = s + 'px';
  applyStyleToSelection('fontSize', s + 'px');
}

function execCmd(cmd) {
  restoreSelection();
  document.execCommand(cmd, false, null);
  saveSelection();
}

// ═══════════════════════════════════════════
//  THEME / BG
// ═══════════════════════════════════════════
function setTheme(id, cls) {
  currentTheme = id;
  bgColorOverride = null;
  gradientActive = false;
  const paper = document.getElementById('letterPaper');
  paper.className = 'letter-paper pixel-border ' + cls;
  paper.style.background = '';
  applyBgPattern();
}

function setBgColor(c) {
  bgColorOverride = c;
  gradientActive = false;
  const paper = document.getElementById('letterPaper');
  // strip theme class but keep letter-paper and pixel-border
  paper.className = 'letter-paper pixel-border';
  paper.style.background = c;
  applyBgPattern();
}

function applyGradient() {
  const c1 = document.getElementById('gradColor1').value;
  const c2 = document.getElementById('gradColor2').value;
  gradientActive = true;
  bgColorOverride = null;
  const paper = document.getElementById('letterPaper');
  paper.className = 'letter-paper pixel-border';
  paper.style.background = `linear-gradient(${gradDir}, ${c1}, ${c2})`;
}

function clearGradient() {
  gradientActive = false;
  const paper = document.getElementById('letterPaper');
  paper.style.background = '';
  setTheme(currentTheme, 'theme-' + currentTheme);
}

function setGradDir(btn, dir) {
  gradDir = dir;
  document.querySelectorAll('.grad-dir-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  if (gradientActive) applyGradient();
}

function applyBgPattern() {
  const paper = document.getElementById('letterPaper');
  const patterns = {
    none: '',
    lines: 'repeating-linear-gradient(transparent, transparent 27px, rgba(0,0,0,0.1) 28px)',
    dots: 'radial-gradient(circle, rgba(0,0,0,0.15) 1px, transparent 1px)',
    grid: 'linear-gradient(rgba(0,0,0,0.07) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,0.07) 1px, transparent 1px)',
    checker: 'repeating-conic-gradient(rgba(0,0,0,0.04) 0% 25%, transparent 0% 50%)',
    diagonal: 'repeating-linear-gradient(45deg, rgba(0,0,0,0.04) 0px, rgba(0,0,0,0.04) 1px, transparent 1px, transparent 10px)',
    hex: 'radial-gradient(circle farthest-side at 0% 50%, transparent 23.5%, rgba(0,0,0,0.05) 24%, rgba(0,0,0,0.05) 26%, transparent 27%), radial-gradient(circle farthest-side at 100% 50%, transparent 23.5%, rgba(0,0,0,0.05) 24%, rgba(0,0,0,0.05) 26%, transparent 27%)',
  };
  const sizes = { none:'', lines:'100% 28px', dots:'20px 20px', grid:'20px 20px', checker:'20px 20px', diagonal:'14px 14px', hex:'40px 70px' };
  if (patterns[bgPattern]) {
    const cur = paper.style.backgroundImage;
    if (gradientActive || bgColorOverride) {
      // overlay pattern on top of color via pseudo — skip for simplicity, just apply
    }
    paper.style.backgroundImage = patterns[bgPattern];
    if (sizes[bgPattern]) paper.style.backgroundSize = sizes[bgPattern];
  } else {
    if (!gradientActive && !bgColorOverride) paper.style.backgroundImage = '';
  }
}

function setBgPattern(p) {
  bgPattern = p;
  applyBgPattern();
}

const BORDERS = {
  classic: {border:'4px solid #000', boxShadow:'0 0 0 4px #b8a882, 8px 8px 0 #000'},
  gold: {border:'4px solid #f9c22b', boxShadow:'0 0 0 4px #c49a1b, 8px 8px 0 #000'},
  diamond: {border:'4px solid #5de8f0', boxShadow:'0 0 0 4px #2abccb, 8px 8px 0 #000'},
  redstone: {border:'4px solid #e83232', boxShadow:'0 0 0 4px #8b1a1a, 8px 8px 0 #000'},
  enchant: {border:'4px solid #9933ff', boxShadow:'0 0 0 4px #5a1a8b, 8px 8px 0 #000'},
  dashed: {border:'4px dashed #000', boxShadow:'8px 8px 0 #000'},
  double: {border:'6px double #000', boxShadow:'8px 8px 0 #000'},
  none: {border:'none', boxShadow:'none'},
};

function setBorder(b) {
  borderStyle = b;
  const paper = document.getElementById('letterPaper');
  const s = BORDERS[b];
  paper.style.border = s.border;
  paper.style.boxShadow = s.boxShadow;
}

// ═══════════════════════════════════════════
//  CANVAS SIZE
// ═══════════════════════════════════════════
function setCanvasSize(w, h) {
  const paper = document.getElementById('letterPaper');
  paper.style.width = w + 'px';
  paper.style.height = h + 'px';
  document.getElementById('customW').value = w;
  document.getElementById('customH').value = h;
  document.getElementById('canvasInfo').textContent = `Canvas: ${w} × ${h} px`;
}

function applyCustomSize() {
  const w = parseInt(document.getElementById('customW').value);
  const h = parseInt(document.getElementById('customH').value);
  if (w >= 200 && h >= 200) {
    setCanvasSize(w, h);
    document.querySelectorAll('.ratio-btn').forEach(b => b.classList.remove('active'));
    showNotif(`Canvas set to ${w}×${h}! ✓`);
  } else {
    showNotif('Minimum size: 200×200px', true);
  }
}

// ═══════════════════════════════════════════
//  STICKER
// ═══════════════════════════════════════════
function addSticker(emoji) {
  const paper = document.getElementById('letterPaper');
  const size = parseInt(document.getElementById('stickerSize').value);
  const el = document.createElement('div');
  el.className = 'letter-sticker';
  el.dataset.rotation = '0';
  el.dataset.flip = '0';

  // inner text span
  const span = document.createElement('span');
  span.textContent = emoji;
  span.style.fontSize = size + 'px';
  span.style.lineHeight = '1';
  span.style.display = 'block';
  el.appendChild(span);

  el.style.left = (40 + Math.random() * Math.max(0, paper.offsetWidth - size - 80)) + 'px';
  el.style.top = (40 + Math.random() * Math.max(0, paper.offsetHeight - size - 80)) + 'px';

  const del = document.createElement('button');
  del.className = 'del-btn';
  del.textContent = '×';
  del.onclick = (e) => { e.stopPropagation(); el.remove(); if (selectedSticker===el) selectedSticker=null; };
  el.appendChild(del);

  // Canva-style rotation handle
  const rotHandle = document.createElement('div');
  rotHandle.className = 'rotate-handle';
  rotHandle.title = 'Drag to rotate';
  el.appendChild(rotHandle);
  attachRotateHandle(rotHandle, el);

  el.addEventListener('mousedown', (e) => {
    if (e.target === del || e.target === rotHandle) return;
    selectSticker(el);
    startDrag(e);
  });
  el.addEventListener('touchstart', (e) => {
    if (e.target === del || e.target === rotHandle) return;
    selectSticker(el);
    startDrag(e);
  }, {passive:false});

  paper.appendChild(el);
}

function addImage(evt) {
  const file = evt.target.files[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = e => placeImage(e.target.result);
  reader.readAsDataURL(file);
  evt.target.value = '';
}

function addImageUrl() {
  const url = document.getElementById('imgUrl').value.trim();
  if (!url) return;
  placeImage(url);
  document.getElementById('imgUrl').value = '';
}

function placeImage(src) {
  const paper = document.getElementById('letterPaper');

  const el = document.createElement('div');
  el.className = 'img-sticker';
  el.dataset.rotation = '0';
  el.style.left = '40px';
  el.style.top  = '40px';

  const img = document.createElement('img');
  img.style.width = '80px';
  img.style.height = 'auto';
  img.src = src;
  img.draggable = false;
  el.appendChild(img);

  // Selection border overlay
  const selBox = document.createElement('div');
  selBox.className = 'img-selection-box';
  el.appendChild(selBox);

  // 8 resize handles
  const handleDirs = ['nw','n','ne','w','e','sw','s','se'];
  handleDirs.forEach(dir => {
    const h = document.createElement('div');
    h.className = 'img-handle ' + dir;
    h.dataset.dir = dir;
    el.appendChild(h);
    attachImgHandle(h, el, img, dir);
  });

  // Rotation line + handle
  const rotLine = document.createElement('div');
  rotLine.className = 'img-rot-line';
  el.appendChild(rotLine);

  const rotHandle = document.createElement('div');
  rotHandle.className = 'img-rot-handle';
  rotHandle.textContent = '↻';
  rotHandle.title = 'Drag to rotate';
  el.appendChild(rotHandle);
  attachImgRotate(rotHandle, el);

  // Delete button
  const del = document.createElement('div');
  del.className = 'img-del-btn';
  del.textContent = '×';
  del.onclick = (e) => { e.stopPropagation(); el.remove(); if (selectedImgSticker === el) selectedImgSticker = null; };
  el.appendChild(del);

  // Drag to move
  el.addEventListener('mousedown', (e) => {
    if (e.target !== el && e.target !== img) return;
    selectImgSticker(el);
    startImgDrag(e, el);
  });
  el.addEventListener('touchstart', (e) => {
    if (e.target !== el && e.target !== img) return;
    selectImgSticker(el);
    startImgDrag(e, el);
  }, {passive: false});

  // Click to select
  el.addEventListener('mousedown', (e) => { selectImgSticker(el); });

  paper.appendChild(el);

  img.onload = () => {
    img.style.width = '80px';
    img.style.height = 'auto';
    const pw = paper.offsetWidth, ph = paper.offsetHeight;
    el.style.left = Math.max(10, Math.min(pw - 100, 40 + Math.random() * (pw - 120))) + 'px';
    el.style.top  = Math.max(10, Math.min(ph - 100, 40 + Math.random() * (ph - 120))) + 'px';
    syncImgSlider(img);
  };
  // fallback if already cached
  if (img.complete && img.naturalWidth) {
    img.style.width = '80px';
    img.style.height = 'auto';
  }

  selectImgSticker(el);
}

// ── IMG STICKER STATE ──
let selectedImgSticker = null;

function selectImgSticker(el) {
  // deselect previous
  if (selectedImgSticker && selectedImgSticker !== el) {
    selectedImgSticker.classList.remove('img-selected');
  }
  selectedImgSticker = el;
  el.classList.add('img-selected');
  syncImgSlider(el.querySelector('img'));
  // switch to sticker tab so slider is visible
  switchTab('sticker');
}

function deselectImgSticker() {
  if (selectedImgSticker) {
    selectedImgSticker.classList.remove('img-selected');
    selectedImgSticker = null;
  }
}

function syncImgSlider(img) {
  if (!img) return;
  const w = Math.round(img.offsetWidth || 80);
  document.getElementById('stickerSize').value = Math.min(w, 400);
  document.getElementById('stickerSizeDisplay').textContent = w + 'px';
}

// ── DRAG TO MOVE ──
function startImgDrag(e, el) {
  e.preventDefault();
  e.stopPropagation();
  const paper = document.getElementById('letterPaper');
  const client = e.touches ? e.touches[0] : e;
  const startX = client.clientX, startY = client.clientY;
  const startL = parseFloat(el.style.left) || 0;
  const startT = parseFloat(el.style.top)  || 0;

  function onMove(ev) {
    ev.preventDefault();
    const c = ev.touches ? ev.touches[0] : ev;
    el.style.left = (startL + c.clientX - startX) + 'px';
    el.style.top  = (startT + c.clientY - startY) + 'px';
  }
  function onEnd() {
    document.removeEventListener('mousemove', onMove);
    document.removeEventListener('mouseup', onEnd);
    document.removeEventListener('touchmove', onMove);
    document.removeEventListener('touchend', onEnd);
  }
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup', onEnd);
  document.addEventListener('touchmove', onMove, {passive: false});
  document.addEventListener('touchend', onEnd);
}

// ── 8-HANDLE RESIZE ──
function attachImgHandle(handle, el, img, dir) {
  handle.addEventListener('mousedown', (e) => startHandleResize(e, el, img, dir));
  handle.addEventListener('touchstart', (e) => startHandleResize(e, el, img, dir), {passive: false});
}

function startHandleResize(e, el, img, dir) {
  e.stopPropagation();
  e.preventDefault();
  const client = e.touches ? e.touches[0] : e;
  const startX  = client.clientX, startY = client.clientY;
  const startW  = img.offsetWidth  || 150;
  const startH  = img.offsetHeight || 150;
  const startL  = parseFloat(el.style.left) || 0;
  const startT  = parseFloat(el.style.top)  || 0;
  const aspect  = startW / startH;

  function onMove(ev) {
    ev.preventDefault();
    const c = ev.touches ? ev.touches[0] : ev;
    const dx = c.clientX - startX;
    const dy = c.clientY - startY;
    let newW = startW, newH = startH, newL = startL, newT = startT;

    if (dir === 'se') { newW = Math.max(20, startW + dx); newH = newW / aspect; }
    else if (dir === 'sw') { newW = Math.max(20, startW - dx); newH = newW / aspect; newL = startL + (startW - newW); }
    else if (dir === 'ne') { newW = Math.max(20, startW + dx); newH = newW / aspect; newT = startT + (startH - newH); }
    else if (dir === 'nw') { newW = Math.max(20, startW - dx); newH = newW / aspect; newL = startL + (startW - newW); newT = startT + (startH - newH); }
    else if (dir === 'e')  { newW = Math.max(20, startW + dx); newH = newW / aspect; }
    else if (dir === 'w')  { newW = Math.max(20, startW - dx); newH = newW / aspect; newL = startL + (startW - newW); }
    else if (dir === 's')  { newH = Math.max(20, startH + dy); newW = newH * aspect; }
    else if (dir === 'n')  { newH = Math.max(20, startH - dy); newW = newH * aspect; newT = startT + (startH - newH); }

    img.style.width  = newW + 'px';
    img.style.height = newH + 'px';
    el.style.left = newL + 'px';
    el.style.top  = newT + 'px';

    document.getElementById('stickerSize').value = Math.min(Math.round(newW), 400);
    document.getElementById('stickerSizeDisplay').textContent = Math.round(newW) + 'px';
  }
  function onEnd() {
    document.removeEventListener('mousemove', onMove);
    document.removeEventListener('mouseup', onEnd);
    document.removeEventListener('touchmove', onMove);
    document.removeEventListener('touchend', onEnd);
  }
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup', onEnd);
  document.addEventListener('touchmove', onMove, {passive: false});
  document.addEventListener('touchend', onEnd);
}

// ── ROTATION ──
function attachImgRotate(handle, el) {
  handle.addEventListener('mousedown', (e) => startImgRotate(e, el));
  handle.addEventListener('touchstart', (e) => startImgRotate(e, el), {passive: false});
}

function startImgRotate(e, el) {
  e.stopPropagation();
  e.preventDefault();
  const rect = el.getBoundingClientRect();
  const cx = rect.left + rect.width / 2;
  const cy = rect.top  + rect.height / 2;
  const client = e.touches ? e.touches[0] : e;
  const startAngle = Math.atan2(client.clientY - cy, client.clientX - cx) * 180 / Math.PI;
  const startRot   = parseFloat(el.dataset.rotation || '0');

  function onMove(ev) {
    ev.preventDefault();
    const c = ev.touches ? ev.touches[0] : ev;
    const r2 = el.getBoundingClientRect();
    const cx2 = r2.left + r2.width / 2, cy2 = r2.top + r2.height / 2;
    let angle = Math.atan2(c.clientY - cy2, c.clientX - cx2) * 180 / Math.PI;
    let newRot = startRot + (angle - startAngle);
    if (ev.shiftKey) newRot = Math.round(newRot / 45) * 45;
    newRot = ((newRot % 360) + 360) % 360;
    el.dataset.rotation = newRot;
    el.style.transform = `rotate(${newRot}deg)`;
    document.getElementById('stickerRotation').value = Math.round(newRot);
    document.getElementById('rotationVal').textContent = Math.round(newRot) + '°';
  }
  function onEnd() {
    document.removeEventListener('mousemove', onMove);
    document.removeEventListener('mouseup', onEnd);
    document.removeEventListener('touchmove', onMove);
    document.removeEventListener('touchend', onEnd);
  }
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup', onEnd);
  document.addEventListener('touchmove', onMove, {passive: false});
  document.addEventListener('touchend', onEnd);
}

// old stub kept for compat
function attachImgResizeHandle() {}

function setBgImage(evt) {
  const file = evt.target.files[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = e => {
    document.getElementById('letterPaper').style.backgroundImage = `url(${e.target.result})`;
    document.getElementById('letterPaper').style.backgroundSize = 'cover';
    document.getElementById('letterPaper').style.backgroundPosition = 'center';
  };
  reader.readAsDataURL(file);
}

function clearBgImage() {
  const paper = document.getElementById('letterPaper');
  paper.style.backgroundImage = '';
  paper.style.backgroundSize = '';
  applyBgPattern();
}

// ═══════════════════════════════════════════
//  DRAG
// ═══════════════════════════════════════════
function startDrag(e) {
  e.preventDefault();
  draggingEl = e.currentTarget;
  draggingEl.classList.add('dragging');
  const paper = document.getElementById('letterPaper');
  const elRect = draggingEl.getBoundingClientRect();
  const clientX = e.touches ? e.touches[0].clientX : e.clientX;
  const clientY = e.touches ? e.touches[0].clientY : e.clientY;
  dragOffX = clientX - elRect.left;
  dragOffY = clientY - elRect.top;
}

function onDragMove(e) {
  if (!draggingEl) return;
  e.preventDefault();
  const paper = document.getElementById('letterPaper');
  const rect = paper.getBoundingClientRect();
  const clientX = e.touches ? e.touches[0].clientX : e.clientX;
  const clientY = e.touches ? e.touches[0].clientY : e.clientY;
  let x = clientX - rect.left - dragOffX;
  let y = clientY - rect.top - dragOffY;
  draggingEl.style.left = Math.max(0, x) + 'px';
  draggingEl.style.top = Math.max(0, y) + 'px';
}

function onDragEnd() {
  if (draggingEl) draggingEl.classList.remove('dragging');
  draggingEl = null;
}

// ═══════════════════════════════════════════
//  EFFECTS
// ═══════════════════════════════════════════
function addSparkle() {
  const sparkles = ['✨','⭐','🌟','💫','⚡'];
  for (let i = 0; i < 5; i++) {
    setTimeout(() => {
      const paper = document.getElementById('letterPaper');
      const s = document.createElement('div');
      s.textContent = sparkles[Math.floor(Math.random()*sparkles.length)];
      s.style.cssText = `position:absolute;font-size:${16+Math.random()*24}px;left:${Math.random()*90}%;top:${Math.random()*90}%;pointer-events:none;animation:float 1.5s forwards;z-index:50`;
      paper.appendChild(s);
      setTimeout(() => s.remove(), 1600);
    }, i * 150);
  }
}

function addParticles() {
  const confetti = ['🎊','🎉','🎈','🌟','💥','🎀','✨','💎'];
  for (let i = 0; i < 12; i++) {
    setTimeout(() => {
      const paper = document.getElementById('letterPaper');
      const s = document.createElement('div');
      s.textContent = confetti[Math.floor(Math.random()*confetti.length)];
      s.style.cssText = `position:absolute;font-size:${14+Math.random()*20}px;left:${Math.random()*95}%;top:${Math.random()*95}%;pointer-events:none;animation:float ${1+Math.random()}s forwards;z-index:50`;
      paper.appendChild(s);
      setTimeout(() => s.remove(), 2000);
    }, i * 100);
  }
}

function updateColorFromSelection() {}

// ═══════════════════════════════════════════
//  TABS
// ═══════════════════════════════════════════
function switchTab(tab) {
  ['text','sticker','bg','canvas'].forEach(t => {
    document.getElementById('tab-'+t).style.display = t===tab ? 'block' : 'none';
  });
  document.querySelectorAll('.side-tab').forEach((b,i) => {
    b.classList.toggle('active', ['text','sticker','bg','canvas'][i]===tab);
  });
}

// ═══════════════════════════════════════════
//  PREVIEW / SHARE
// ═══════════════════════════════════════════
function showPreview() {
  const paper = document.getElementById('letterPaper');
  const to = document.getElementById('toField').value || '?';
  const from = document.getElementById('fromField').value || '?';
  const previewLetter = document.getElementById('previewLetter');
  previewLetter.innerHTML = `
    <div style="font-family:'Press Start 2P',monospace;font-size:9px;color:#888;margin-bottom:8px">TO: ${escHtml(to)}</div>
    <div style="border:4px solid #000;box-shadow:4px 4px 0 #000;min-height:300px;position:relative;overflow:hidden">
      ${paper.outerHTML.replace(/contenteditable="true"/g, 'contenteditable="false"')}
    </div>
    <div style="font-family:'Press Start 2P',monospace;font-size:9px;color:#888;margin-top:8px;text-align:right">FROM: ${escHtml(from)}</div>
  `;
  document.getElementById('previewModal').classList.add('open');
  if (shareLink) document.getElementById('shareBox').textContent = shareLink;
}

function closeModal() {
  document.getElementById('previewModal').classList.remove('open');
}

function escHtml(s) {
  return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
}

// ═══════════════════════════════════════════════════════════════
//  SUPABASE SHARE SYSTEM — true cross-device sharing
//  Letters are stored in Supabase (free, up to 500MB).
//  Anyone can open the link on ANY device, ANY browser.
//
//  ── ONE-TIME SETUP (5 min, completely free) ──────────────────
//  1. Go to https://supabase.com → "Start your project" (free)
//  2. Create a new project (name/region/password don't matter)
//  3. Go to Table Editor → + New Table:
//       Name: letters
//       Columns:
//         id         uuid  primary key  default: gen_random_uuid()
//         data       text  (not null)
//         created_at timestamptz       default: now()
//  4. Go to Authentication → Policies → letters table:
//       + New policy → "Enable insert for everyone" (role: anon)
//       + New policy → "Enable select for everyone" (role: anon)
//  5. Go to Project Settings → API:
//       Copy "Project URL"    → paste as SUPABASE_URL below
//       Copy "anon / public"  → paste as SUPABASE_ANON_KEY below
//  6. Save this file and open it. Done — sharing works globally!
//
//  ⚠ The anon key is safe to put here — it's intentionally public.
//    Row Level Security (your insert+select policies) controls access.
// ═══════════════════════════════════════════════════════════════

const SUPABASE_URL      = 'https://jkcsevcikfinxwdawcng.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImprY3NldmNpa2Zpbnh3ZGF3Y25nIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzc3NzczNTgsImV4cCI6MjA5MzM1MzM1OH0.dwWdzxnIjokbKZTZ15e9Si65Sg4BDz2LlXm6W5bifys';

async function saveLetter(data) {
  const res = await fetch(`${SUPABASE_URL}/rest/v1/letters`, {
    method: 'POST',
    headers: {
      'Content-Type':  'application/json',
      'apikey':        SUPABASE_ANON_KEY,
      'Authorization': `Bearer ${SUPABASE_ANON_KEY}`,
      'Prefer':        'return=representation',
    },
    body: JSON.stringify({ data: JSON.stringify(data) }),
  });
  if (!res.ok) {
    const err = await res.text().catch(() => res.status);
    throw new Error(`Supabase save failed (${res.status}): ${err}`);
  }
  const rows = await res.json();
  if (!rows || !rows[0] || !rows[0].id) throw new Error('No ID returned from Supabase');
  return rows[0].id; // UUID like "a1b2c3d4-e5f6-..."
}

async function fetchLetter(id) {
  const res = await fetch(
    `${SUPABASE_URL}/rest/v1/letters?id=eq.${encodeURIComponent(id)}&select=data&limit=1`,
    {
      headers: {
        'apikey':        SUPABASE_ANON_KEY,
        'Authorization': `Bearer ${SUPABASE_ANON_KEY}`,
      },
    }
  );
  if (!res.ok) throw new Error(`Supabase fetch failed: ${res.status}`);
  const rows = await res.json();
  if (!rows || rows.length === 0) throw new Error('Letter not found');
  return JSON.parse(rows[0].data);
}

function generateLink() {
  if (SUPABASE_URL === 'YOUR_SUPABASE_PROJECT_URL') {
    showNotif('Set up Supabase first! See code comments.', true);
    return;
  }

  const paper = document.getElementById('letterPaper');
  const to    = document.getElementById('toField').value;
  const from  = document.getElementById('fromField').value;
  const textContent = document.getElementById('textContent').innerHTML;

  const stickers = [];
  paper.querySelectorAll('.letter-sticker').forEach(s => {
    const span = s.querySelector('span');
    stickers.push({
      type: 'emoji',
      text: span ? span.textContent : s.childNodes[0]?.textContent || '',
      left: s.style.left, top: s.style.top,
      size: span ? span.style.fontSize : '32px',
      rotation: s.dataset.rotation || '0',
      flip: s.dataset.flip || '0',
    });
  });

  paper.querySelectorAll('.img-sticker').forEach(s => {
    const img = s.querySelector('img');
    if (!img) return;
    stickers.push({
      type: 'image',
      src: img.src,
      left: s.style.left, top: s.style.top,
      width:  img.style.width  || img.offsetWidth  + 'px',
      height: img.style.height || img.offsetHeight + 'px',
      rotation: s.dataset.rotation || '0',
    });
  });

  const data = {
    v: 5,
    to, from, textContent,
    theme: currentTheme,
    border: borderStyle,
    stickers,
    canvasW: paper.style.width  || '560px',
    canvasH: paper.style.height || '460px',
    bgStyle: paper.getAttribute('style') || '',
  };

  const json = JSON.stringify(data);
  const mb = (json.length / 1024 / 1024).toFixed(1);
  if (json.length > 5 * 1024 * 1024) {
    showNotif(`Letter too large (${mb}MB). Try smaller images.`, true);
    return;
  }

  const btn = document.getElementById('generateLinkBtn');
  if (btn) { btn.textContent = '⏳ SAVING...'; btn.disabled = true; }
  showNotif('Saving letter... ✨');

  saveLetter(data).then(id => {
    const url = window.location.origin + window.location.pathname + '?id=' + id;
    shareLink = url;
    document.getElementById('shareBox').textContent = url;
    if (btn) { btn.textContent = '🔗 GENERATE SHARE LINK'; btn.disabled = false; }
    copyLink();
    showPreview();
  }).catch(err => {
    console.error('Save error:', err);
    if (btn) { btn.textContent = '🔗 GENERATE SHARE LINK'; btn.disabled = false; }
    showNotif('Save failed! Check Supabase setup in code.', true);
  });
}

function copyLink() {
  if (!shareLink) { showNotif('Generate a link first!', true); return; }
  navigator.clipboard.writeText(shareLink).then(() => {
    showNotif('Link copied! Share it with anyone 🎉');
  }).catch(() => {
    const t = document.getElementById('shareBox');
    const range = document.createRange();
    range.selectNode(t);
    window.getSelection().removeAllRanges();
    window.getSelection().addRange(range);
    showNotif('Select the link above and copy manually!');
  });
}

// ─────────────────────────────────────────────────────────────────
//  READ-ONLY LETTER VIEW  (rendered when ?id= is in the URL)
// ─────────────────────────────────────────────────────────────────
function renderLetterView(data) {
  document.body.innerHTML = '';
  document.body.style.cssText = "margin:0;padding:0;background:#1a0f05;min-height:100vh;display:flex;flex-direction:column;align-items:center;font-family:'Press Start 2P',monospace";

  // Header
  const hdr = document.createElement('div');
  hdr.style.cssText = 'text-align:center;padding:24px 16px 12px;width:100%';
  hdr.innerHTML = `
    <div style="font-size:clamp(16px,3vw,28px);color:#f9c22b;text-shadow:4px 4px 0 #c49a1b,8px 8px 0 #000;letter-spacing:2px;animation:pixelPulse 2s ease-in-out infinite">✉ PixelLetter ✉</div>
    <div style="font-family:'VT323',monospace;font-size:20px;color:#a0d8a0;margin-top:6px">► A magical letter, crafted just for you ◄</div>
    <style>@keyframes pixelPulse{0%,100%{text-shadow:4px 4px 0 #c49a1b,8px 8px 0 #000}50%{text-shadow:4px 4px 0 #c49a1b,8px 8px 0 #000,0 0 20px #f9c22b}}</style>`;
  document.body.appendChild(hdr);

  // To/From bar
  const toFrom = document.createElement('div');
  toFrom.style.cssText = 'background:#3d2e1e;border:4px solid #000;box-shadow:4px 4px 0 #000;padding:10px 18px;margin:0 16px 12px;display:flex;gap:16px;align-items:center;flex-wrap:wrap;max-width:960px;width:calc(100% - 32px)';
  toFrom.innerHTML = `
    <span style="font-size:7px;color:#888">TO:</span>
    <span style="font-family:'VT323',monospace;font-size:22px;color:#e0d4b8">${escHtml(data.to || '—')}</span>
    <span style="color:#6e5a3e;margin:0 4px">·</span>
    <span style="font-size:7px;color:#888">FROM:</span>
    <span style="font-family:'VT323',monospace;font-size:22px;color:#e0d4b8">${escHtml(data.from || '—')}</span>`;
  document.body.appendChild(toFrom);

  // Paper wrapper + paper
  const wrapper = document.createElement('div');
  wrapper.style.cssText = 'padding:8px;background:#d4c5a0;border:4px solid #000;box-shadow:4px 4px 0 #000,inset 0 0 0 2px #b8a882;margin:0 16px 16px;max-width:100%;overflow:auto';

  const paper = document.createElement('div');
  const themeClass = (THEMES.find(t => t.id === data.theme) || THEMES[0]).class;
  paper.className = 'letter-paper pixel-border ' + themeClass;

  if (data.bgStyle) {
    const tmp = document.createElement('div');
    tmp.setAttribute('style', data.bgStyle);
    ['background','backgroundColor','backgroundImage','backgroundSize','backgroundPosition'].forEach(p => {
      if (tmp.style[p]) paper.style[p] = tmp.style[p];
    });
  }
  paper.style.width    = data.canvasW || '560px';
  paper.style.height   = data.canvasH || '460px';
  paper.style.position = 'relative';
  paper.style.overflow = 'hidden';

  if (data.border && BORDERS[data.border]) {
    paper.style.border    = BORDERS[data.border].border;
    paper.style.boxShadow = BORDERS[data.border].boxShadow;
  }

  // Text content
  const tc = document.createElement('div');
  tc.style.cssText = 'position:absolute;top:0;left:0;right:0;bottom:0;z-index:2;line-height:1.4;word-break:break-word;white-space:pre-wrap;padding:24px;height:100%;overflow-y:auto;pointer-events:none';
  tc.innerHTML = data.textContent || '';
  paper.appendChild(tc);

  // Stickers
  (data.stickers || []).forEach(s => {
    const el = document.createElement('div');
    if (s.type === 'image') {
      el.style.cssText = `position:absolute;left:${s.left};top:${s.top};z-index:10;transform:rotate(${parseFloat(s.rotation)||0}deg);transform-origin:center center`;
      const img = document.createElement('img');
      img.src = s.src;
      img.style.cssText = `display:block;width:${s.width||'80px'};height:${s.height||'auto'};max-width:none;pointer-events:none`;
      img.draggable = false;
      el.appendChild(img);
    } else {
      const flip = s.flip === '1' ? ' scaleX(-1)' : '';
      el.style.cssText = `position:absolute;left:${s.left};top:${s.top};z-index:10;transform:rotate(${parseFloat(s.rotation)||0}deg)${flip};transform-origin:center center`;
      const span = document.createElement('span');
      span.textContent = s.text;
      span.style.cssText = `font-size:${s.size||'32px'};line-height:1;display:block`;
      el.appendChild(span);
    }
    paper.appendChild(el);
  });

  wrapper.appendChild(paper);
  document.body.appendChild(wrapper);

  // Footer
  const footer = document.createElement('div');
  footer.style.cssText = 'display:flex;gap:10px;flex-wrap:wrap;justify-content:center;padding:0 16px 32px';
  footer.innerHTML = `
    <a href="${window.location.pathname}" style="font-family:'Press Start 2P',monospace;font-size:8px;color:#fff;background:#3d7a3d;padding:10px 14px;text-decoration:none;border:3px solid #000;box-shadow:4px 4px 0 #000">✏ WRITE YOUR OWN</a>
    <button id="copyLinkBtn" onclick="navigator.clipboard.writeText(location.href).then(()=>{document.getElementById('copyLinkBtn').textContent='✓ COPIED!'}).catch(()=>{})"
      style="font-family:'Press Start 2P',monospace;font-size:8px;color:#2d1e0f;background:#c49a1b;padding:10px 14px;border:3px solid #000;box-shadow:4px 4px 0 #000;cursor:pointer">
      🔗 COPY LINK
    </button>`;
  document.body.appendChild(footer);
}

function showLoadingScreen() {
  document.body.innerHTML = `
    <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:100vh;font-family:'Press Start 2P',monospace;color:#f9c22b;text-align:center;padding:24px;background:#1a0f05">
      <div style="font-size:clamp(16px,3vw,28px);text-shadow:4px 4px 0 #c49a1b">✉ PixelLetter ✉</div>
      <div style="margin-top:24px;font-size:10px;color:#a0d8a0;animation:blink 1s step-end infinite">Opening your letter...</div>
      <style>@keyframes blink{0%,100%{opacity:1}50%{opacity:0}}</style>
    </div>`;
}

function showErrorScreen(msg) {
  document.body.innerHTML = `
    <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:100vh;font-family:'Press Start 2P',monospace;color:#f9c22b;text-align:center;padding:24px;background:#1a0f05">
      <div style="font-size:clamp(16px,3vw,28px);text-shadow:4px 4px 0 #c49a1b">✉ PixelLetter ✉</div>
      <div style="margin-top:24px;font-size:11px;color:#e83232">Oops!</div>
      <div style="margin-top:12px;font-size:7px;color:#a09070;line-height:2.2;max-width:380px">${msg}</div>
      <a href="${window.location.pathname}" style="margin-top:24px;font-family:'Press Start 2P',monospace;font-size:8px;color:#fff;background:#3d7a3d;padding:10px 16px;text-decoration:none;border:3px solid #000;box-shadow:4px 4px 0 #000">✏ CREATE NEW LETTER</a>
    </div>`;
}

function loadFromId() {
  const id = new URLSearchParams(window.location.search).get('id');
  if (!id) return;
  showLoadingScreen();

  if (SUPABASE_URL === 'YOUR_SUPABASE_PROJECT_URL') {
    showErrorScreen('Supabase is not configured yet.<br><br>Open the HTML file and follow the 5-step setup instructions in the code comments near the top of the &lt;script&gt; block.');
    return;
  }

  fetchLetter(id)
    .then(data => renderLetterView(data))
    .catch(err => {
      console.error('Load error:', err);
      showErrorScreen('This letter could not be loaded.<br><br>' + escHtml(err.message));
    });
}

// ── Entry point ──
if (new URLSearchParams(window.location.search).get('id')) {
  loadFromId();
} else {
  init();
}
</script>
</body>
</html>